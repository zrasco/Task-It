@model NotificationsAndLayoutViewModel
@{
    ViewData["Title"] = "Layout";
    ViewData.AddActivePage(ManageNavPages.NotificationsAndLayout);
}

@Html.Partial("_StatusMessage", Model.StatusMessage)
<div class="row">
    <div class="col-md-6">
        <form method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            @*<h4>Notification settings</h4>
            
            <div class="form-group">
                <div class="checkbox">
                    <label>
                        <input asp-for="@Model.EmailOnFeedUpdate" id="emailOnFeedUpdate" /> @Html.DisplayNameFor(model => model.EmailOnFeedUpdate)
                    </label>
                </div>
                <div class="checkbox">
                    <label>
                        <input asp-for="@Model.EmailOnMessage" id="emailOnFeedUpdate" /> @Html.DisplayNameFor(model => model.EmailOnMessage)
                    </label>
                </div>
                <div class="checkbox">
                    <label>
                        <input asp-for="@Model.TextOnFeedUpdate" id="emailOnFeedUpdate" /> @Html.DisplayNameFor(model => model.TextOnFeedUpdate)
                    </label>
                </div>
                <div class="checkbox">
                    <label>
                        <input asp-for="@Model.TextOnMessage" id="emailOnFeedUpdate" /> @Html.DisplayNameFor(model => model.TextOnMessage)
                    </label>
                </div>
            </div>*@
            <h4>Layout settings</h4>
            <div class="form-group">
                <label asp-for="@Model.HomeScreen"></label>
                <select asp-for="@Model.HomeScreen" id="homeScreen">
                    <option value="@HomeScreen.Feed">Feed</option>
                    <option value="@HomeScreen.Journal">Task Manager</option>
                    <option value="@HomeScreen.Subscriptions">Subscriptions</option>
                    <option value="@HomeScreen.Achievements">Achievements</option>
                </select>
            </div>
            <!--
            <div class="form-group">
                <label asp-for="Username"></label>
                <input asp-for="Username" class="form-control" disabled />
            </div>
            <div class="form-group">
                <label asp-for="PhoneNumber"></label>
                <input asp-for="PhoneNumber" class="form-control" />
                <span asp-validation-for="PhoneNumber" class="text-danger"></span>
            </div>
            -->

            <button type="submit" class="btn btn-default">Save</button>
        </form>
    </div>
</div>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}
